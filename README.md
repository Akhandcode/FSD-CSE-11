# FSD-CSE-11
fsd repository




 $git --version $git config -- global user.name $git config -- global user.email $git config -- list $git clone url $cd fsd-cse_11 $git brach *main$ $git branch dev-name $git checkout dev-name $code $git status $git add . $git status $git diff $git commit -m "commit js file" $git push origin dev-name
 $git --version $git config -- global user.name $git config -- global user.email $git config -- list $git clone url $cd fsd-cse_11 $git brach *main$ $git branch dev-name